# Архітектура та реалізація застосунку (MVVM на Flutter)

## 1. Загальний опис проєкту

Метою роботи було створення мобільного застосунку на Flutter, який керує списком користувачів: додавання, редагування, видалення, валідація даних, робота з аватаром та перемикання теми оформлення.

Архітектура побудована за шаблоном **MVVM (Model–View–ViewModel)**, що забезпечує чітке розділення логіки, стану та інтерфейсу. Застосунок працює повністю локально: дані зберігаються у `SharedPreferences` у вигляді JSON.

## 2. Структура проєкту

lib/

  models/
  
    user.dart
  viewmodels/
  
    users_view_model.dart
    theme_view_model.dart
  services/
  
    user_storage_service.dart
    theme_storage_service.dart
    image_service.dart
  views/
  
    home_screen.dart
  widgets/
  
    users_list.dart
    user_form.dart



- **models** — моделі даних  
- **viewmodels** — бізнес-логіка та управління станами  
- **services** — робота з пам’яттю, темою, файлами  
- **views** — інтерфейс користувача та окремі віджети  

---

## 3. Model (Модель)

### `User`
Містить структуру даних одного користувача:

- `id`
- `name`
- `email`
- `phone`
- `photoUri`

Додатково реалізовано:

- `toJson()` / `fromJson()` — серіалізація та десеріалізація  
- `copyWith()` — створення змінених копій об’єкта без модифікації оригіналу  

---

## 4. ViewModel (Управління логікою та станом)

### `UsersViewModel`

Відповідає за:

- список користувачів  
- додавання та редагування  
- видалення  
- валідацію полів  
- перевірку унікальності `name` та `email`  
- збереження даних у локальне сховище  

Використовує **ChangeNotifier**, що дозволяє View автоматично оновлюватися після змін стану.

---

### `ThemeViewModel`

Функції:

- зберігання поточного режиму теми  
- перемикання між `light` / `dark` / `system`  
- збереження вибору користувача у `SharedPreferences`  

---

## 5. Services (Сервіси)

### `UserStorageService`

Робота з локальним сховищем:

- зберігання списку користувачів у JSON  
- завантаження списку при старті застосунку  

---

### `ThemeStorageService`

Зберігає режим теми під ключем `"theme_mode"` зі значеннями:

- `"light"`
- `"dark"`
- `"system"`

---

### `ImageService`

Використовує `image_picker`:

- дозволяє обрати фото з галереї  
- повертає шлях до зображення  
- використовується для аватарів користувачів  

---

## 6. View (Інтерфейс)

### `HomeScreen`

Містить:

- AppBar з кнопкою перемикання теми  
- список користувачів (`UsersList`)  
- кнопку додавання нового користувача  
- відкриття діалогової форми (`UserForm`)  

---

### `UserForm`

Діалогова форма, що підтримує:

- введення імені  
- email  
- телефону  
- вибір аватару (`Обрати фото`)  
- валідацію всіх полів  
- повідомлення про помилки (AlertDialog)  

---

### `UsersList`

Компонент списку:

- відображає аватар або першу букву імені  
- підтримує редагування й видалення  
- використовує `FileImage` для локальних фото  

---

## 7. Валідація даних

Виконується у `UsersViewModel`:

- ім’я не може бути порожнім  
- email — має валідний формат + повинен бути унікальним  
- телефон — 7–15 цифр  
- заборонені дублікати `name` та `email`  

У разі помилки:

- з’являється вікно `AlertDialog` з поясненням  

---

## 8. Збереження даних

Список користувачів зберігається у `SharedPreferences` у вигляді JSON:

```json
[
  {
    "id": "123456",
    "name": "ivan",
    "email": "ivan@example.com",
    "phone": "+3801234567",
    "photoUri": "/storage/emulated/0/DCIM/avatar.jpg"
  }
]
```

Дані автоматично завантажуються при запуску застосунку.

---

## 9. Перемикання теми

Користувач може натиснути іконку ☀️/🌙 у верхньому меню.

- керування темою відбувається через **ThemeViewModel**  
- вибір зберігається у **SharedPreferences**  
- після перезапуску застосунок відкривається у тій темі, що була обрана  

---

## 10. Підсумок

У застосунку реалізовано повну **MVVM-архітектуру**:

- **Model** — структура даних  
- **ViewModel** — логіка, валідація, управління станами  
- **View** — інтерфейс, що реагує на зміни  
- **Services** — робота з темою, файлами та локальним сховищем  

### Переваги підходу:

- масштабованість  
- чистий, зрозумілий та підтримуваний код  
- можливість легко додавати нові функції  
- логічне відділення UI від бізнес-логіки  

