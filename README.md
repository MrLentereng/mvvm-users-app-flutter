Архітектура та реалізація застосунку (MVVM на Flutter)
1. Загальний опис проєкту

Метою роботи було створення мобільного застосунку на Flutter, який керує списком користувачів:
додавання, редагування, видалення, валідація даних, робота з аватаром та перемикання теми оформлення.

Архітектура побудована за шаблоном MVVM (Model–View–ViewModel), що забезпечує чітке розділення логіки, стану та інтерфейсу.
Застосунок працює повністю локально: дані зберігаються у SharedPreferences у вигляді JSON.

2. Структура проєкту
lib/
  models/
    user.dart
  viewmodels/
    users_view_model.dart
    theme_view_model.dart
  services/
    user_storage_service.dart
    theme_storage_service.dart
    image_service.dart
  views/
    home_screen.dart
    widgets/
      users_list.dart
      user_form.dart


models — моделі даних

viewmodels — бізнес-логіка та управління станами

services — робота з пам’яттю, темою, файлами

views — UI та окремі віджети

3. Model (Модель)
User

Містить структуру даних одного користувача:

id

name

email

phone

photoUri

У моделі реалізовано:

toJson() / fromJson() — серіалізація

copyWith() — створення змінених копій об’єкта

4. ViewModel (Управління логікою та станом)
UsersViewModel

Відповідає за:

список користувачів

додавання та редагування

видалення користувачів

валідацію полів

перевірку унікальності name та email

збереження даних у локальне сховище

Використовує ChangeNotifier, що дозволяє View автоматично оновлюватися.

ThemeViewModel

Функції:

зберігання поточного режиму теми

перемикання між light / dark / system

збереження вибору користувача у SharedPreferences

5. Services (Сервіси)
UserStorageService

Робота з локальним сховищем:

зберігання списку користувачів як JSON

завантаження при старті застосунку

ThemeStorageService

Зберігає ключ "theme_mode" зі значенням:

"light"

"dark"

"system"

ImageService

Використовує пакет image_picker:

дозволяє вибрати фото з галереї

повертає локальний шлях до файлу для аватару

6. View (Інтерфейс)
HomeScreen

Містить:

AppBar з кнопкою перемикання теми

список користувачів (UsersList)

кнопку додавання нового користувача

відкриття форми редагування (UserForm)

UserForm

Діалогова форма:

Ім’я

Email

Телефон

Аватар + кнопка «Обрати фото»

Валідація даних

Вікно помилки у разі дублікатів

UsersList

Компонент списку:

аватар або перша літера імені

кнопки редагування та видалення

підтримка фото через FileImage

7. Валідація даних

Валідація виконується у UsersViewModel:

Ім’я не може бути порожнім

Email — валідний формат + унікальність

Телефон — тільки цифри, 7–15 символів

Заборонені дублікати ім’я та email

У разі помилки формується AlertDialog з повідомленням.

8. Збереження даних

Список користувачів зберігається в SharedPreferences у вигляді JSON:

[
  {
    "id": "123456",
    "name": "ivan",
    "email": "ivan@example.com",
    "phone": "+3801234567",
    "photoUri": "/storage/emulated/0/DCIM/avatar.jpg"
  }
]


Дані завантажуються при запуску застосунку.

9. Перемикання теми

Користувач натискає на іконку ☀️/🌙 у верхньому меню.

Тема оновлюється через ThemeViewModel

Значення зберігається у SharedPreferences

При повторному запуску застосунок відображає останню вибрану тему

10. Підсумок

У застосунку повністю реалізовано MVVM-архітектуру:

Model — структура даних

ViewModel — логіка, валідація, управління станом

View — інтерфейс, що реагує на зміни стану

Services — робота з файлами, темою, локальним сховищем

Архітектура забезпечує:

масштабованість

чистий код

можливість легко додавати нові функції

незалежність UI від бізнес-логіки